name: Go

on:
  push:
  pull_request:
  workflow_dispatch:
  create:
  delete:

jobs:
  build-1:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4
      - name: Sleep and Print
        run: |
          sleep 20
          echo 1111

  build-2:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4
      - name: Sleep and Print
        run: |
          sleep 20
          echo 222222222222
